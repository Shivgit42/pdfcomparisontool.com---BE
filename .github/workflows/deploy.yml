- name: Create archive (from git HEAD)
  run: |
    TARFILE=deploy_package.tar.gz
    rm -f "$TARFILE"
    # create archive from the current commit (HEAD). This does not include node_modules or untracked files.
    git archive --format=tar.gz -o "$TARFILE" HEAD
